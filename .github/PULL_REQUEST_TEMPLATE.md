## What this changes

## Why

## Rules source
If this touches game rules, link or quote the SRD section.

## Checklist
- [ ] Edited `sheet.html` and ran `sh build.sh` so `index.html` is updated
- [ ] Opened `index.html` in a browser and checked the change in both Play and Edit mode
- [ ] Existing saved characters still load (no renamed or removed fields without a migration in `normalize()`)
